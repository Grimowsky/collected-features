version: '3.8'

services:
  frontend:
    build:
      context: ../../
      dockerfile: ./apps/test-2/Dockerfile
    volumes:
      - ./src:/app/apps/test-2/src
      - ../../libs/shared-ui/src:/app/libs/shared-ui/src
    working_dir: /app/apps/test-2
    command: "npm run dev"
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - VITE_API_URL=http://localhost:8080

